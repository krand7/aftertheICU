- content_for :title, 'Learn About the ICU'
- content_for :description, 'Read and learn about important topics for ICU survivors, family members, researchers, and clinicians'

.container
  - @content_groups.each do |content_group|
    %br
    %h1.white= content_group.title
    %br
    - content_group.content_pages.each do |content_page|
      = render 'learn/content_page_thumbnail', content_page: content_page, admin_view: false
    - unless content_group == @content_groups.last
      %hr

%br
