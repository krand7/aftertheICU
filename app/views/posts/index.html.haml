%p#notice= notice
%h1 Listing Posts
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - @posts.each do |post|
      %tr
        %td= link_to 'Show', forum_topic_post(@forum, @topic)
        %td= link_to 'Edit', edit_forum_topic_post_path(@forum, @topic, post)
        %td= link_to 'Destroy', forum_topic_post(@forum, @topic), method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Post', new_forum_topic_post_path(@forum, @topic)
