%p#notice= notice
%h1 Listing Topics
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - @topics.each do |topic|
      %tr
        %td= link_to 'Show', [@forum, topic]
        %td= link_to 'Edit', edit_forum_topic_path(@forum, topic)
        %td= link_to 'Destroy', [@forum, topic], method: :delete, data: { confirm: 'Are you sure?' }
%br
= link_to 'New Topic', new_forum_topic_path(@forum), class: 'btn btn-primary'
